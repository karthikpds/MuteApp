{
  "name": "appmute",
  "version": "0.1.0",
  "description": "Mute or pause audio from specific apps — per-application audio control for Windows",
  "main": "main.js",
  "author": "AppMute",
  "license": "MIT",
  "scripts": {
    "start": "electron .",
    "test": "node --test \"tests/*.test.js\"",
    "dist": "electron-builder",
    "dist:win": "electron-builder --win"
  },
  "devDependencies": {
    "electron": "^33.4.0",
    "electron-builder": "^25.1.8"
  },
  "build": {
    "appId": "com.appmute.app",
    "productName": "AppMute",
    "files": [
      "main.js",
      "preload.js",
      "renderer/**/*",
      "lib/**/*",
      "native/**/*",
      "resources/tray.png",
      "resources/tray@2x.png",
      "resources/app-icon.png",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "native/windows/",
        "to": "native/windows/",
        "filter": ["*.exe"]
      }
    ],
    "win": {
      "target": ["nsis"]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    }
  }
}
